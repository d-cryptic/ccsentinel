use anyhow::Result;

pub async fn run() -> Result<()> {
    // TUI placeholder — will be implemented with ratatui in task #6
    println!("Interactive TUI coming soon. Use `cst list` for now.");
    Ok(())
}
