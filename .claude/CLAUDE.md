# CLAUDE.md — claude-sentinel (local .claude override)

This project has its own CLAUDE.md at the repo root. See `/CLAUDE.md`.

## Quick Reference

- Binary: `cst` (cargo install --path crates/cst-cli)
- Shared lib: `cst-core` (crates/cst-core/)
- Tests: `cargo nextest run`
- Watch: `cargo watch -x 'nextest run'`

## Active Skills

- `cst-domain` — domain model reference
- `rust-tdd` — TDD patterns for Rust
- `cargo-workspace` — workspace patterns
